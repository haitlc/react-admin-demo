import React, { Component } from 'react';

import styled from 'styled-components';

class Form extends Component {
  render() {
    return (
      <div>
        <span>The Form component</span>
      </div>
    );
  }
}

export default Form;
